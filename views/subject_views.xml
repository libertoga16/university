<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_subject_form" model="ir.ui.view">
        <field name="name">university.subject.form</field>
        <field name="model">university.subject</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                         <button name="%(action_university_enrollment)d" type="action" class="oe_stat_button" icon="fa-pencil-square-o" context="{'default_subject_id': id, 'search_default_subject_id': id}">
                            <field name="enrollment_count" widget="statinfo" string="Enrollments"/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                            <field name="department_id"/>
                            <field name="university_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Professors">
                            <field name="professor_ids"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_subject_tree" model="ir.ui.view">
        <field name="name">university.subject.tree</field>
        <field name="model">university.subject</field>
        <field name="arch" type="xml">
            <list>
                <field name="code"/>
                <field name="name"/>
                <field name="department_id"/>
                <field name="university_id"/>
            </list>
        </field>
    </record>

    <record id="view_subject_kanban" model="ir.ui.view">
        <field name="name">university.subject.kanban</field>
        <field name="model">university.subject</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                            </div>
                            <div class="o_kanban_record_body">
                                <field name="university_id"/>
                            </div>
                        </div>
                    </t>
                </templates>
                
            </kanban>
        </field>
    </record>

     <record id="view_subject_search" model="ir.ui.view">
        <field name="name">university.subject.search</field>
        <field name="model">university.subject</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="code"/>
                <field name="department_id"/>
                <field name="university_id"/>
            </search>
        </field>
    </record>

    <record id="action_university_subject" model="ir.actions.act_window">
        <field name="name">Subjects</field>
        <field name="res_model">university.subject</field>
        <field name="view_mode">list,form,kanban</field>
    </record>
</odoo>
