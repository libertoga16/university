<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- FORM VIEW -->
    <record id="university_professor_view_form" model="ir.ui.view">
        <field name="name">university.professor.view.form</field>
        <field name="model">university.professor</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <!-- Header: Avatar y Botones Estadísticos -->
                    <field name="image_1920" widget="image" class="oe_avatar" options="{'preview_image': 'image_128'}"/>
                    <div class="oe_button_box" name="button_box">
                         <button name="%(action_university_enrollment)d" type="action" class="oe_stat_button" icon="fa-pencil-square-o" context="{'default_professor_id': id, 'search_default_professor_id': id}">
                            <field name="enrollment_count" widget="statinfo" string="Enrollments"/>
                        </button>
                    </div>
                    
                    <!-- Información Principal -->
                    <group>
                        <group name="main_info">
                            <field name="name"/>
                            <field name="email" widget="email"/>
                            <field name="university_id"/>
                        </group>
                        <group name="deps">
                            <field name="department_id"/>
                        </group>
                    </group>
                    
                    <!-- Notebook: Relaciones y Detalles Extra -->
                    <notebook>
                        <page string="Subjects" name="subjects">
                            <field name="subject_ids"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- LIST VIEW -->
    <record id="university_professor_view_list" model="ir.ui.view">
        <field name="name">university.professor.view.list</field>
        <field name="model">university.professor</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="university_id"/>
                <field name="department_id"/>
            </list>
        </field>
    </record>

    <!-- KANBAN VIEW -->
    <record id="university_professor_view_kanban" model="ir.ui.view">
        <field name="name">university.professor.view.kanban</field>
        <field name="model">university.professor</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="id"/>
                <field name="image_128"/>
                <templates>
                    <t t-name="card">
                        <!-- Kanban Card Content -->
                        <div class="d-flex align-items-center">
                            <div class="me-3">
                                <field name="image_128" widget="image" class="o_image_64_max rounded shadow-sm"/>
                            </div>
                            <div class="d-flex flex-column">
                                <field name="name" class="fw-bolder fs-5"/>
                                <span class="text-muted"><field name="department_id"/></span>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    
    <!-- SEARCH VIEW -->
    <record id="university_professor_view_search" model="ir.ui.view">
        <field name="name">university.professor.view.search</field>
        <field name="model">university.professor</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="university_id"/>
                <field name="department_id"/>
                <filter string="University" name="group_university" context="{'group_by':'university_id'}"/>
            </search>
        </field>
    </record>


</odoo>
